name: Organisation Setup Contract

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  contract:
    name: Organisation setup administrator contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run organisation setup contract
        run: node scripts/organisation-setup-admin.test.mjs
